#include "main.h"

/**
* _isdigit - checks for digit character
* @c: Returns 1 if digit 0 otherwise
* Return: 1 or 0
*/

int _isdigit(int c)
{
	if (c >= 48 && c <= 57)
	{
	return (1);
	}
	return (0);
}
