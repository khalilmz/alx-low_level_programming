#include <stdio.h>

/**
 * main -  print all single digit numbers of base 10 from 1 to 9
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;

	for (i = 0 ; i < 10 ; i++)
	{
		printf("%d", i);
	}
	return (0);
}
